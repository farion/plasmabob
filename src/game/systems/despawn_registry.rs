// Despawn registry removed — this module previously provided a small
// resource to deduplicate despawn operations per-frame but it was never
// constructed or used. Keeping the module file present but empty avoids
// dead-code warnings while removing the unused API.

// (intentionally empty)


